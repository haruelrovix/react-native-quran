import { StyleSheet } from 'react-native';
import { COLOR_PALLETE } from '../constants';

export const styles = StyleSheet.create({
  listContainer: {
    backgroundColor: COLOR_PALLETE.crystalWhite,
    marginHorizontal: 10,
    padding: 10,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: COLOR_PALLETE.silverStorm,
  },
});
